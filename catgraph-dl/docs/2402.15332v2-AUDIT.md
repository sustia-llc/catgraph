# Gavranović–Lessard–Dudzik–von Glehn–Araújo–Veličković 2024 Coverage Audit (catgraph-dl v0.3.1)

> **Paper:** Gavranović\*, Lessard\*, Dudzik\*, von Glehn, Araújo, Veličković, *Categorical Deep Learning is an Algebraic Theory of All Architectures* ([arXiv:2402.15332v2](https://arxiv.org/abs/2402.15332), 6 Jun 2024; ICML 2024 PMLR 235:15209-15241; in-tree at [`docs/2402.15332v2.pdf`](2402.15332v2.pdf), 539 KB). Read as "Categorical Deep Learning is an Algebraic {Theory of All Architectures}".
> **Library:** `catgraph-dl` v0.3.1 (Phase DL-3 multi-reviewer pass, 2026-05-06 at SHA `12ad5fd`; co-released with workspace umbrella tag `v0.13.2`). Prior baseline: v0.3.0 (SHA `ac699f6`, umbrella `v0.13.1`). Initial audit at v0.2.0 baseline (SHA `4f8bda8`, umbrella `v0.13.0`).
> **Date:** 2026-05-06 (v0.3.1 deltas appended after the v0.3.0 multi-reviewer-pass post-shipping triage — 8 Important + ride-along Minors closed; v0.3.0 deltas were populated by the deep paper-audit reviewer during the multi-reviewer pass; initial audit produced in support of the v0.3.0 design phase).
> **Method:** read all main-body sections (§1–§4) plus Appendices A, B (incl. B.1), C, H, I, J, K end-to-end; cross-walked every numbered Definition / Example / Remark / Theorem / Equation against `catgraph-dl/src/**` and `catgraph-dl/tests/**`. Companion artefacts consulted (now bundled in-tree at [`2402.15332v2-SUMMARY.md`](2402.15332v2-SUMMARY.md)): the merged paper rendering with cross-checking caveats (Part II of the SUMMARY) and the transcript-vs-paper comparison flagging Hopf-fibration / carry / pathfinding motivations as transcript-only (Part I of the SUMMARY). Source artefacts originated under `~/Documents/sustia/categorical-dl/docs/` as `categorical_deep_learning_merged.md` + `categorical_deep_learning_transcript_vs_paper.md` and were merged into the SUMMARY for full in-tree self-containment. The Filter Equivariants follow-up paper [arXiv:2507.08796v1](https://arxiv.org/abs/2507.08796) (Lewis-Ghani-Dudzik-Perivolaropoulos-Pascanu-Veličković, July 2025) was scanned for Dudzik carry-problem evolution; §6 of that paper explicitly acknowledges ripple-carry as **out of scope** for the FE framework, confirming no Hopf-fibration preprint as of audit date.
>
> **Status legend:**
> - ✅ DONE — implemented and tested against the paper's stated property.
> - ⚠️ PARTIAL — implementation exists but does not fully exhibit the paper's structure (or a corner case is mis-scoped, or a paper-flagged caveat carries forward).
> - ⏭️ DEFERRED — planned for a later release (DL-3+ / v0.3.0+).
> - ➖ N/A — discussion / motivational / explicitly stated as future work in the paper itself.
> - 🔗 IN-APPLIED — implementation lives in `catgraph-applied`; consumed via re-export at `catgraph_dl::*`.
>
> **Companion audits (catgraph workspace):** [`FS19-AUDIT.md`](../../catgraph/docs/FS19-AUDIT.md) (catgraph), [`FS18-AUDIT.md`](../../catgraph-applied/docs/FS18-AUDIT.md) (catgraph-applied), [`BV25-AUDIT.md`](../../catgraph-magnitude/docs/BV25-AUDIT.md) (catgraph-magnitude), [`GORARD23-AUDIT.md`](https://github.com/tsondru/irreducible/blob/main/docs/GORARD23-AUDIT.md) (irreducible).
>
> **⚠️ CAREFUL — Hopf-fibration / carry-operation provenance.** The private `hopf_fibration` namespace in `catgraph-dl/src/hopf_fibration/` reserves room for Andrew Dudzik's transcript-only conjecture about modular-arithmetic carry as a non-trivial S¹-fibration of S³ → S². **This is not a result of the published CDL ICML 2024 paper** and is also not addressed in the published Filter Equivariants follow-up (arXiv:2507.08796, July 2025), which §6 explicitly flags ripple-carry as outside its framework. Treat the Hopf claim as pre-publication research from Dudzik; do not cite as Gavranović et al. material until a preprint exists. The `hopf_fibration` module is intentionally kept private (`mod hopf_fibration;`, not `pub mod`) and is **NOT** audited here.
>
> **⚠️ CAREFUL — Appendix H worked-example caveats carry forward.** The merged-paper rendering in `~/Documents/sustia/categorical-dl/docs/categorical_deep_learning_merged.md` flags index-bookkeeping issues in Appendix H.1 (Z₂ swap equivariance derivation) and the worked invariance derivation embedded in H.3 (rederived from-scratch yields a different constraint than what the summary-document quoted). The structural conclusions (equivariance ⇒ circulant/symmetric matrix ⇒ CNN; invariance ⇒ output is symmetric in inputs) are correct and are what the catgraph-dl tests exercise. The caveat is on the paper's worked-example arithmetic, not on the cg-dl implementation; rederive from your own conventions before citing intermediate steps from H.1 / H.3.

## 1. Summary

| Section | DONE | PARTIAL | DEFERRED | N/A | IN-APPLIED | Total |
|---|---|---|---|---|---|---|
| §1 Introduction (motivational) | 0 | 0 | 0 | 6 | 0 | 6 |
| §1.3.1 Essential Concepts (Defs 1.1, 1.4, 1.5; Exs 1.2, 1.3) | 4 | 1 | 0 | 0 | 0 | 5 |
| §2.1 Monads + Algebras (Defs 2.1, 2.3, 2.5; Exs 2.2, 2.4, 2.6; Rem 2.7) | 7 | 0 | 0 | 0 | 0 | 7 |
| §2.2 Endofunctors + (Co)algebras (Def 2.8 + B.2; Exs 2.9–2.15; Rem 2.13) | 9 | 0 | 0 | 0 | 0 | 9 |
| §2.3 Where to Next? (motivational) | 0 | 0 | 0 | 1 | 0 | 1 |
| §3.1 Para 2-category (Def + Composition + Reparam + Thm G.10 + `SetCategoryDefaults` ZST defaults pattern) | 7 | 0 | 0 | 1 | 0 | 8 |
| §3.2 2-dim categorical algebra (lax / strong / strict) | 1 | 1 | 0 | 1 | 0 | 3 |
| §4 New Horizons (motivational) | 0 | 0 | 0 | 5 | 0 | 5 |
| Appendix A (initial / terminal / (co)limits) | 0 | 0 | 0 | 5 | 0 | 5 |
| Appendix B (Free Monads — Defs B.3, B.8; Prop B.18; Exs B.19, B.20) | 4 | 1 | 0 | 1 | 0 | 6 |
| Appendix C (GDL examples — C.1 Σₙ, C.2 SO(3), C.3 G-on-G) | 1 | 0 | 2 | 0 | 0 | 3 |
| Appendix H (Weight tying examples — H.1, H.3, H.4, H.5, H.7) | 1 | 2 | 1 | 0 | 0 | 4 |
| Appendix I (Parametric 2-endofunctors — I.1–I.5) | 5 | 0 | 0 | 0 | 0 | 5 |
| Appendix J (Unrolling — J.1–J.4) | 4 | 1 | 0 | 0 | 0 | 5 |
| Appendix K (Haskell types) | 5 | 0 | 0 | 0 | 0 | 5 |
| **TOTAL** | **48** | **6** | **3** | **20** | **0** | **77** |

**Headline numbers (as of catgraph-dl v0.3.0):**
- **62% DONE / 8% PARTIAL / 4% DEFERRED / 26% N/A / 0% IN-APPLIED** (rounded; v0.2.0 percentages preserved — the v0.3.0 single-row addition is below the rounding threshold).
- Of 77 audited items, **20 are N/A** (motivational / philosophical / explicit future-work in the paper itself), leaving **57 implementable items** of which **48 are DONE, 6 PARTIAL, 3 DEFERRED**.
- Of implementable items: **84% DONE / 11% PARTIAL / 5% DEFERRED** (was 47/56 at v0.2.0; tightened to 48/57 at v0.3.0 — `SetCategoryDefaults` opt-in marker added as a new DONE row, see §3.1 row below; the four shipped surfaces otherwise document/test-only and do not flip any pre-existing row's status).
- **v0.2.0 baseline (locked numerical anchor for v0.3.0 deltas comparison):** 47 DONE / 6 PARTIAL / 3 DEFERRED of 56 implementable = 83.9% DONE → reported as 84%.
- The largest single-section coverage is §2 + Appendix I + Appendix J + Appendix K (the architecture-as-(co)algebra core thesis): **23 of 24 implementable items DONE**, 1 PARTIAL (J.2 bounded-only `unroll_to_vec`), 0 deferred. The cg-dl crate is paper-faithful on the headline correspondence "five neural architectures = five (co)algebras of named endofunctors".
- The deferred items are: (a) Appendix C.1 Σₙ-on-graphs equivariance recovery (parallel sibling crate `catgraph` already covers graph machinery; no cg-dl test fixture yet), (b) Appendix C.2 SO(3)-on-S² spherical-CNN recovery (linear-algebra heavy; deferred until a `nalgebra` integration story lands), (c) Appendix H.5 truly-infinite stream/coalgebra `unroll_into_iter` semantics (`UnfoldingRnn::unroll_to_vec` is bounded-depth; lazy / Stream-based unrolling deferred to DL-3+).
- The PARTIAL items concentrate on (a) §1.3.1 Def 1.5 Natural Transformation (encoded as functor-law obligation on `EndoFunctor::fmap`, not as a first-class trait), (b) §3.2 lax/strong/strict monoidal distinction (current `MonoidalCategory` trait is silently strict — see ⚠️ CAREFUL section below; v0.3.0 ships a module-level rustdoc rationale for the `&self` slot, narrowing but not closing the gap), (c) Appendix B.3 well-pointedness (a property, not a structure; inherited from `EndoFunctor` law obligations), (d) Appendix H.1 / H.3 worked examples (paper-side caveat carries forward; the cg-dl `Z2Group` GDL recovery test in `tests/algebra_homomorphisms.rs` is paper-faithful and does NOT depend on the H.1 index-bookkeeping arithmetic).

---

## 2. Coverage by paper section

### §1 Introduction — motivational

| Item | Status | Location | Notes |
|---|---|---|---|
| Top-down vs bottom-up framing of architecture specification | ➖ | (narrative) | Background motivation; no implementation expected. |
| Geometric Deep Learning (GDL) as top-down exemplar | ➖ | (narrative) | The catgraph-dl `Z2Group` GDL recovery test is the operationalisation; appears under §2.1 Ex 2.6 below. |
| Bottom-up frameworks (TensorFlow, PyTorch, JAX) | ➖ | (narrative) | No implementation expected. |
| §1.1 "Our Opinion" — gap between top-down and bottom-up | ➖ | (narrative) | No implementation expected. |
| §1.2 "Our Position" — bridge top-down and bottom-up via category theory | ➖ | (narrative) | The crate IS the operationalisation of this thesis, but the position itself is not a numbered formal claim. |
| §1.3 The Power of Category Theory | ➖ | (narrative + bibliography) | Field-spanning bibliographic context; no implementation expected. |

### §1.3.1 Essential Category Theory Concepts

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Def 1.1 Category (objects, morphisms, identity, composition; unit + associativity laws) | §1.3.1 Def 1.1 | ✅ | `src/para/morphism.rs` (`Para<M, C>`) + `src/free_monad/free_mnd.rs` (`FreeMnd<F, Z>` carries category-of-`F`-algebras structure implicitly) | The category-theoretic primitive is encoded as type-level handles (`Para<M, C>` is a `PhantomData` namespace tag; `M` and `C` are themselves trait-bounded markers). Identity / unit-laws / associativity are caller obligations on the underlying Rust closures (no runtime law-checking). |
| Ex 1.2 The Set Category | §1.3.1 Ex 1.2 | ✅ | `src/para/monoidal_category.rs` (`SetMonoidal`) + `src/para/actegory.rs` (`SetActegory`) | `(Set, ×, 1)` is the only concrete instance shipped; objects are Rust types `A: 'static`, morphisms are Rust closures. |
| Ex 1.3 Groups and monoids as one-object categories | §1.3.1 Ex 1.3 | ✅ | `src/algebra/group_action.rs` (`Group` trait + `Z2Group`) | `Z2Group` (cyclic group of order 2 — XOR-on-`bool`) is the concrete instance. The trait surface admits other monoids; only `Z2Group` is shipped. |
| Def 1.4 Functor (object map + morphism map; preserves identity + composition) | §1.3.1 Def 1.4 | ✅ | `src/endofunctor.rs` (`EndoFunctor` trait at crate root) | Encoded via the GAT-based HKT pattern: object map is `type Apply<X>`, morphism map is `fn fmap<X, Y, G: Fn(X) -> Y>`. Functor laws are documented obligations (module-level rustdoc), not machine-checked. Concrete instances: `ListEndo<A>`, `TreeEndo<A>`, `GroupActionEndo<G>`. The pattern mirrors `deep_causality_haft` v0.3.1's `HKT::Type<X>` — see [the hkt-type-system skill reference](#references). |
| Def 1.5 Natural transformation | §1.3.1 Def 1.5 | ⚠️ | (no first-class type) | Naturality squares are a documented obligation, not a runtime-checked structure. The cg-dl pattern is "supply concrete `EndoFunctor` impls; the morphism-map fmap, taken pointwise across the family, *is* a natural transformation between functor instances". A future DL-3+ enrichment could add a `NaturalTransformation<F, G>` carrier (no current consumer demands it). |

### §2.1 Monads and their Algebras

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Def 2.1 Monad (endofunctor `M`, unit `η`, multiplication `μ`; coherence diagrams) | §2.1 Def 2.1 | ✅ | `src/algebra/monad_algebra.rs` (`MonadAlgebra<M, A, S>` carrier; `MonadAlgebraHom`) | The `Monad` triple itself is a *documented obligation* on the `M` type parameter; `MonadAlgebra` carries the algebra structure-map `a : M(A) → A`. Monad coherence laws (unit + associativity, Def B.1) are caller-attested per the `MonadAlgebraHom::new` rustdoc. A separate runtime-checkable `Monad` trait carrying `unit` / `multiplication` lands in DL-3+ when a consumer surfaces. |
| Ex 2.2 Group action monad `(G × −, η, μ)` on `Set` | §2.1 Ex 2.2 | ✅ | `src/algebra/group_action.rs` (`GroupActionEndo<G>`) | Endofunctor instance: `Apply<X> = (G, X)`. The unit `η : id_Set ⇒ G × −` and multiplication `μ : G × G × − ⇒ G × −` are documented as caller obligations on the `Group` impl (Group trait carries `e: identity` + `compose: G × G → G`). |
| Def 2.3 Algebra for a monad `(A, a : M(A) → A)` | §2.1 Def 2.3 | ✅ | `src/algebra/monad_algebra.rs` (`MonadAlgebra<M, A, S>`) | Carrier `A` + structure-map `S: Fn(M::Apply<A>) -> A`. The unit / associativity diagrams (Def 2.3 left/right diagrams) are caller-attested per rustdoc. |
| Ex 2.4 Group actions arise as algebras of `G × −` | §2.1 Ex 2.4 | ✅ | `tests/algebra_homomorphisms.rs` (Z₂ swap action fixture) | Concrete fixture: `Z2Group` acting on `Vec<f64>` by negation (`true ↦ negate, false ↦ id`). Trivial action (projection) also covered — see Ex 2.6 below. |
| Def 2.5 M-algebra homomorphism | §2.1 Def 2.5 | ✅ | `src/algebra/monad_algebra.rs` (`MonadAlgebraHom<M, A, B, FromS, ToS, MapS>`) | Wraps `FAlgebraHom`; F-algebra square is machine-checkable via `verify_commutes(fa: F::Apply<A>) -> bool`. The additional monad-unit + monad-associativity coherence (Def 2.3 laws) is a documented obligation per `MonadAlgebraHom::new` rustdoc. |
| **Ex 2.6 Equivariant maps as monad-algebra homomorphisms (GDL recovery)** | §2.1 Ex 2.6 | ✅ | `tests/algebra_homomorphisms.rs::test_z2_invariant_absolute_value` | **Headline test of the paper's core claim that GDL is a special case of CDL.** Pairs source algebra `(Vec<f64>, negation_action)` with target algebra `(Vec<f64>, trivial_action)`. An F-algebra homomorphism between them is exactly a Z₂-invariant map. The test asserts `\|·\|` (absolute value) IS such a homomorphism (verify_commutes returns `true` on a representative grid: both group elements; positive, negative, zero coordinates; empty vector). A non-equivariant projection (`vec![x[0]]`) is the negative control. Reifies the paper's claim: "we have just successfully derived the key aim of geometric deep learning." |
| Rem 2.7 Monoid actions ⇔ functors `BM → Set` ⇔ algebras for `M × −` (de Haan et al. 2020a) | §2.1 Rem 2.7 | ✅ | `src/algebra/group_action.rs` rustdoc + `Z2Group` instance | Documented framing. The `Group` trait subsumes the monoid case (relax `inverse` to `id` on a one-element group); cg-dl's `Group` is the strict group case to keep `Z2Group` as the canonical Ex 2.6 fixture. |

### §2.2 Endofunctors and their (Co)algebras

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Def 2.8 Algebra for an endofunctor `(A, a : F(A) → A)` (no monad-coherence equations) | §2.2 Def 2.8 | ✅ | `src/algebra/f_algebra.rs` (`FAlgebra<F, A, S>`) | Carrier + structure-map; no extra laws. The contrast with Def 2.3 is preserved: `FAlgebra` carries no unit / associativity verifier. |
| Def B.2 Coalgebra for an endofunctor `(A, a : A → F(A))` (the dual) | §2.2 + App B.2 | ✅ | `src/algebra/coalgebra.rs` (`FCoalgebra<F, A, S>`) | Dual of `FAlgebra`; structure-map points the other way. |
| Ex 2.9 Lists as `(1 + A × −)`-algebra: `[Nil, Cons]` | §2.2 Ex 2.9 | ✅ | `src/free_monad/list_endo.rs` (`ListEndo<A>` with `Apply<X> = Option<(A, X)>`) | Concrete `EndoFunctor` instance. `Option::None` ↔ `Nil` (the `1` summand); `Option::Some((a, x))` ↔ `Cons(a, x)` (the `A × −` summand). |
| Ex 2.10 Binary trees as `(A + (−)²)`-algebra: `[Leaf, Node]` | §2.2 Ex 2.10 | ✅ | `src/free_monad/tree_endo.rs` (`TreeEndo<A>` with `Apply<X> = Either<A, (X, X)>`) | Concrete `EndoFunctor` instance using the workspace `either::Either`. `Left(a)` ↔ `Leaf(a)`; `Right((l, r))` ↔ `Node(l, r)`. |
| Ex 2.11 Mealy machines as `(I → O × −)`-coalgebra | §2.2 Ex 2.11 | ✅ | `src/architectures/mealy_cell.rs` (`MealyCell<P, S, I, O, F>`) | Coalgebra structure-map shape: `Cell: Fn((P, S)) -> Step` where `Step: FnOnce(I) -> (O, S)`. The two-stage closure is the standard Rust workaround for "function returning closure". `MealyCell::run` exercises the coalgebra structurally over an input list. |
| Ex 2.12 Folds over lists as algebra homomorphisms | §2.2 Ex 2.12 | ✅ | `src/architectures/folding_rnn.rs` (`FoldingRnn::unroll`) | Right-fold semantics implemented as `inputs.into_iter().rev().fold(cell_0(p), step)` (Haskell `foldr` convention). Verified equivariant via the `FreeMnd`-equivalence test (see Rem 2.13 below). |
| Rem 2.13 Initial-algebra uniqueness: `(List(A), [Nil, Cons])` is initial in `Alg(1 + A × −)`, so there is only one algebra homomorphism from lists to any `(X, [r₀, r₁])` | §2.2 Rem 2.13 | ✅ | `tests/architecture_unrollers.rs::test_folding_rnn_freemnd_equivalence` + `test_recursive_nn_freemnd_equivalence` | **The unroller IS the unique algebra homomorphism from the initial algebra of the free monad.** Both tests assert `unroll(cell, x) == unroll_via_free_mnd(cell, to_free_mnd(x))` on multiple samples — direct reification of the paper's central claim. The list direction uses `vec_to_free_mnd`; the tree direction uses `tree_to_free_mnd` with `Infallible` as the never-leaf terminator. Caller-sampled (not exhaustive); proptest extension deferred to DL-3+. |
| Ex 2.14 Tree folds as algebra homomorphisms | §2.2 Ex 2.14 | ✅ | `src/architectures/recursive_nn.rs` (`RecursiveNn::unroll`) | Post-order tree walk: `Leaf(_) → cell_0(p)`; `Node(l, r) → cell_1((p, a, l_val, r_val))`. The four-arg `cell_1` shape requires an `A` payload at internal-node combination; `BinaryTree::Node` carries no node payload, so the unroller takes the leftmost-leaf payload by convention (documented; tests use payload-agnostic cells so unobservable). DL-3+ option to widen `Node` to carry an `A` payload — see CHANGELOG v0.2.0 Phase DL-2 Agent E notes. |
| Ex 2.15 Unfolds as coalgebra homomorphisms | §2.2 Ex 2.15 | ✅ | `src/architectures/unfolding_rnn.rs` (`UnfoldingRnn::unroll_to_vec`) + `src/architectures/mealy_cell.rs` (`MealyCell::run`) + `src/architectures/moore_cell.rs` (`MooreCell::run`) | Three coalgebra examples covered; bounded-depth only (lazy/Stream semantics deferred to DL-3+). |

### §2.3 Where to Next? — motivational

| Item | Status | Location | Notes |
|---|---|---|---|
| Motivation for 2-categories (need to model parameters + non-linear maps) | ➖ | (narrative) | Operationalised by §3.1 onward. |

### §3.1 The 2-category Para — headline architectural surface

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Para 2-category construction `Para_▶(C)` from monoidal category `(M, ⊗, I)` and `M`-actegory `C` (Cruttwell et al. 2022; Capucci et al. 2022) | §3.1 (boxed defn) | ✅ | `src/para/mod.rs` (module-level rustdoc) + `src/para/morphism.rs` (`Para<M, C>` namespace tag) | The 2-category itself is a type-level handle; objects / 1-morphisms / 2-morphisms are concrete value-level types. |
| Objects of `Para_▶(C)` = objects of `C` (in `Set` for the default) | §3.1 | ✅ | `src/para/monoidal_category.rs` (`SetObject` marker) + `src/para/actegory.rs` (`SetActegory::Object`) | Objects of `Set` are Rust types `A: 'static`; the trait surface uses kind-of-objects markers (`SetObject`, `SetMorphism`) to keep `MonoidalCategory::Object` typed without committing to one concrete object type. |
| 1-morphisms `(P, f) : X → Y` with `P ∈ M`, `f : P ▶ X → Y` | §3.1 | ✅ | `src/para/morphism.rs` (`ParaMorphism<M, C, P, F>`) | Carries `parameter: P` + `action: F` where `F: Fn((P, X)) -> Y`. The tuple-input convention `Fn((P, X)) -> Y` matches the architectures scaffold. |
| 2-morphisms `(P, f) ⇒ (P', f')` via `r : P' → P` (parameter substitution triangle) | §3.1 | ✅ | `src/para/reparameterization.rs` (`Reparameterization<M, R>` + `apply` method) | `Reparameterization::apply` precomposes `r` to substitute `r(p')` for `p` in the original `f`. Diagonal `Δ : P → (P, P)` specialises to weight tying — see Theorem G.10 row below. **v0.4.0 update (2026-05-10):** `Reparameterization::apply` widened to 6-param `<C, PNew, POld, F, X, Y>` with `C: Actegory<SetMonoidal>` at leftmost position; body structurally agnostic — see `tie_weights` row below for the cascade-closing widening. |
| Sequential composition `(Q ⊗ P, h)` with `h = g ∘ (Q ▶ f) ∘ μ` | §3.1 (composition equation) | ✅ | `src/para/morphism.rs` (`ParaMorphism::compose`) | Composes via `h((q, p), x) = g((q, f((p, x))))`. The `μ` coherence is exact in `SetActegory` (canonical tuple re-association `(q, (p, x)) ↦ ((q, p), x)`); the generic shape is enforced via `Actegory::compose_action`. Tested in `tests/para_composition.rs::test_compose_numeric`. **v0.4.0 update (2026-05-10):** `ParaMorphism::compose` impl block widened to `impl<C, P, F> ParaMorphism<SetMonoidal, C, P, F> where C: Actegory<SetMonoidal>`; body structurally identical to v0.3.1 (the destructure + closure don't dispatch through the actegory). |
| Theorem G.10 — lax (co)algebras for `Para(T)` induce comonoids in `M`; `δ` = comultiplication, `ε` = counit, comonoid laws follow from lax algebra coherence | §3.1 (Thm G.10 boxed) | ✅ | `src/para/comonoid.rs` (`Comonoid<M>` trait + `DiagonalComonoid<M>` witness) + `src/para/comonoid.rs::tie_weights` (consumer-facing API) | **Headline weight-tying result.** `DiagonalComonoid<SetMonoidal>` provides `δ(p) = (p.clone(), p)` and `ε(p) = ()`. Coassociativity / left-counit / right-counit laws are exact equalities in `Set` (not "up to iso") and are asserted in `tests/comonoid_laws.rs` via 6 acceptance tests (proptest-driven on `i32` and `String`). The `tie_weights` function is the consumer-facing API: takes a `ParaMorphism<…, (P, P), F>` and returns the diagonal-tied `ParaMorphism<…, P, _>` — this is the API consumed by `catgraph-coalition` v0.4.0 for parameter-sharing in coalition-actegory dispatch. v0.3.0 added an end-to-end consumption simulation in `tests/coalition_consumption_simulation.rs` (defines a local `MockQuantale: Actegory<SetMonoidal>` ZST, exercises `tie_weights::<i64, _, i64, i64>(3, untied)` end-to-end, and cross-validates `MockQuantale::act` against `SetActegory::act` pointwise). The `tie_weights` signature is still `SetActegory`-bound at v0.3.0 — actegory-genericity is a v0.4.0+ Architectural item (see §3 below). **v0.4.0 update (2026-05-10):** `tie_weights` widened to `<C, P, F, X, Y>` with `C: Actegory<SetMonoidal>` at the leftmost generic position; the body is now structurally agnostic to the actegory choice, matching CDL §3.1 Theorem G.10's statement. **Paper-fidelity precision (v0.4.1):** the comonoid `(δ, ε)` lives in `M` (the monoidal-category parameter), NOT in the actegory `C`. The widening is sufficient because the diagonal construction in `tie_weights` does not reference `C::ActionResult` or `C::Tensor`; it operates purely at the `M = SetMonoidal` level. Cascade widening: `Reparameterization::apply` (6-param `<C, PNew, POld, F, X, Y>`) + `ParaMorphism::compose` (impl-block-level `<C, P, F>` with `C: Actegory<SetMonoidal>`). Non-source-breaking for inference-relying callers; explicit-turbofish callers move from 4-param to 5-param on `tie_weights`. The `coalition_consumption_simulation.rs` simulation now uses 5-param `tie_weights::<SetActegory, i64, _, i64, i64>(...)`. |
| Graphical formalism (string-diagram notation for parameters drawn vertically; sequential composition fuses parameter spaces in parallel) | §3.1 | ➖ | (narrative + ASCII diagrams in rustdoc) | No graphical-DSL implementation. Module-level rustdoc retains the ASCII string-diagram convention from the paper for documentation purposes only. |
| `(Set, ×, 1)`-flavoured ZST defaults pattern (audit-row added at v0.3.0 — paper does not call this pattern explicitly but it operationalises CDL §3.1's choice of `(Set, ×, 1)` as the default monoidal category for Para(M, C)) | §3.1 (CDL default `(Set, ×, 1)`) | ✅ | `src/para/monoidal_category.rs` (`SetCategoryDefaults` opt-in marker trait + `impl<T: SetCategoryDefaults> MonoidalCategory for T` blanket) | **v0.3.0 Surface (b).** Opt-in marker trait carrying the canonical Cartesian-product / tuple-re-association method bodies. Downstream `(Set, ×, 1)`-flavoured naming-witness ZSTs (e.g., a future `MyMonoidal: SetCategoryDefaults`) get `MonoidalCategory` for free with an empty trait body, instead of reproducing `tensor_objects` / `unit` / `associate` / `left_unitor` / `right_unitor` pointwise. `SetMonoidal` itself opts into the blanket via empty `impl SetCategoryDefaults for SetMonoidal {}` — pointwise-identical behaviour to the v0.2.0 hand-written impl. Implementation took option (γ-ii) blanket-via-opt-in-marker — the originally-sketched (γ-i) sub-trait-with-default-method-bodies form does not type-check on stable Rust because a sub-trait cannot override supertrait method bodies. Rationale documented in `src/para/monoidal_category.rs` module rustdoc + `SetCategoryDefaults` doctest. The blanket fixes `Object = SetObject` and `Morphism = SetMorphism`; non-`(Set, ×, 1)`-flavoured ZSTs should `impl MonoidalCategory` directly. **v0.4.0 update (2026-05-10):** soft-sealed via `private::Sealed` to surface the `(Set, ×, 1)`-commitment at impl site (option (a) from v0.4.0 forward-look §1.4). Downstream users must dual-impl `Sealed + SetCategoryDefaults` to opt in; `SetMonoidal` itself adopts the dual-impl pattern. The seal is "soft" — a deliberate `Sealed` impl + hand-rolled `MonoidalCategory` impl is the documented bypass path the v0.3.1 conflict-guard caveat still warns against. Migration impact: zero — no external downstream consumer at v0.4.0 design time; coalition v0.5.0 will adopt the sealed pattern from the start. |

### §3.2 2-dimensional categorical algebra

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Square commutativity flavours in 2-categories: commute / pseudo-commute / lax-commute / oplax-commute | §3.2 | ➖ | (narrative) | Conceptual taxonomy; not a separate type. |
| Lax algebras for `Para(T)` are sufficient to derive recursive / recurrent NNs from first principles (1-cocycle connection to Dudzik et al. 2024 asynchronous NNs) | §3.2 | ✅ | `src/architectures/*` (all five unrollers) | The lax-algebra structure is the operational substrate of the architecture unrollers. The 1-cocycle / asynchronous-NN connection (Dudzik-von Glehn-Pascanu-Veličković 2024, LoG) is referenced in the merged-paper notes and the cg-dl `hopf_fibration` private namespace stub but is not directly exercised by current tests. |
| Lax / strong / strict monoidal functor distinction (paper line 27, after Eq 65) | §3.2 + Eqs after 65 | ⚠️ | `src/para/monoidal_category.rs` (silently strict; v0.3.0 rustdoc-improved) | The current `MonoidalCategory` trait body provides exact (strict) coherence isomorphisms for `SetMonoidal`. Other DL-3+ instances over richer monoidal categories (R-modules, hyperdoctrine) will need lax / strong / strict naming distinction. **v0.3.0 Surface (a) shipped:** new module-level rustdoc paragraph "Why methods take `&self`" in `src/para/monoidal_category.rs` documents that the `&self` slot is reserved for DL-3+ runtime-data carriers (R-module instances carrying their base ring; hyperdoctrine instances carrying their fibration projection; vector-bundle instances carrying their connection) — freezing the trait at static methods today would force a breaking change later. Status remains ⚠️ PARTIAL because the lax / strong / strict trait-level distinction itself is still deferred — the v0.3.0 rustdoc improves the *documentation* of the deferral but does not resolve the structural gap (no `LaxMonoidalCategory` / `StrongMonoidalCategory` / `StrictMonoidalCategory` trait family at v0.3.0; deeper trait-level distinction is DL-4+). |

### §4 New Horizons — motivational

| Item | Status | Location | Notes |
|---|---|---|---|
| Generalised equivariance (parametric + lax algebras → verifiably correct logic / code) | ➖ | (narrative) | Vision statement; future research direction. |
| Containers and type-safe design (polynomial endofunctors → containers) | ➖ | (narrative) | Future direction. The `EndoFunctor` GAT-witness pattern is the substrate; a `Container` trait could be added in DL-3+ if a consumer surfaces. |
| Beyond individual layers (architectural blocks spanning multiple layers; nonlinearity treatment) | ➖ | (narrative) | Future direction. |
| Equitable AI systems (architectural imposition of protected-class invariance via categorical logic) | ➖ | (narrative) | Future direction. |
| Verifiable AI as the path to compliance + safety | ➖ | (narrative) | Future direction. |

### Appendix A (Category-theory basics)

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Def A.1 Equivalence of categories | App A | ➖ | — | No catgraph-dl consumer; covered by `catgraph` core where applicable. |
| Def A.2 Initial object | App A | ➖ | — | Used informally in the Rem 2.13 argument; no first-class `InitialObject` type in cg-dl. |
| Def A.3 Terminal object | App A | ➖ | — | Symmetric; no first-class type. |
| Def A.4 Limit (cone construction) | App A | ➖ | — | No catgraph-dl consumer. |
| Def A.5 Colimit (cocone, dual) | App A | ➖ | — | No catgraph-dl consumer. |

### Appendix B — 1-Categorical Algebra (incl. Free Monads)

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Def B.1 Monad coherence diagrams (unit + associativity) | App B.1 | ➖ | (caller obligation) | Coherence is documented obligation on `MonadAlgebraHom::new`; no machine-checked verifier. |
| Def B.3 Pointed / well-pointed endofunctor (`σ : id_C ⇒ F` with whiskering coherence) | App B.1 Def B.3 | ⚠️ | (functor-law obligation only) | Well-pointedness is a property of the natural-transformation-from-identity, not a structure carried by `EndoFunctor`. The cg-dl pattern is "supply concrete `EndoFunctor` impls satisfying the functor laws; well-pointedness is a downstream consumer concern." A future `Pointed<F>` trait (analogous to HAFT's `Pure<F>`) lands in DL-3+ when a consumer surfaces. |
| Def B.8 Algebraically free monad on `F` (equivalence `Alg_Endo(F) ≅ Alg_Mnd(FreeMnd(F))`) | App B.1 Def B.8 | ✅ | `src/free_monad/free_mnd.rs` (`FreeMnd<F, Z>` is exactly the algebraically-free-monad construction) | The `FreeMnd<F, Z>` type IS the algebraic-free-monad witness. The equivalence-of-categories itself is a documented theorem, not a machine-checked structure. |
| **Prop B.18 — `FreeMnd(F)(Z) = Fix(X ↦ F(X) + Z)` and `CofreeCmnd(F)(Z) = Fix(X ↦ F(X) × Z)`** | App B.1 Prop B.18 | ✅ | `src/free_monad/free_mnd.rs` (`FreeMnd<F, Z>` enum: `Pure(Z) \| Roll(Box<F::Apply<FreeMnd<F, Z>>>)`) + `src/free_monad/cofree_cmnd.rs` (`CofreeCmnd<F, Z>` struct: `head: Z, tail: Box<F::Apply<CofreeCmnd<F, Z>>>`) | **Headline construction enabling the architecture-as-(co)algebra unrollers.** Rust 1.95 + Edition 2024 compiles the recursive type with `Box` indirection through the GAT projection cleanly. Hand-rolled `Clone`/`Debug`/`PartialEq`/`Eq` impls with explicit `where F::Apply<Self>: Clone/Debug/...` bounds (the `derive` macros' inferred bounds don't always discharge through GAT projection). |
| Ex B.19 Free monad on `1 + A × −` is `List_{−+1}(A)` (lists with last-element type `Z + 1` instead of `1`) | App B.1 Ex B.19 | ✅ | `src/free_monad/list_endo.rs` (`vec_to_free_mnd` + `free_mnd_to_vec` bijection) | Bijection witness `FreeMnd<ListEndo<A>, Z> ≅ (Vec<A>, Z)`. Verified in `tests/free_monad_bijections.rs` via 64-case proptest round-trip on `Vec<u32>` plus an empty-list-to-`Pure(())` corner case plus a hand-built `[1, 2]` cons-cell tower. |
| Ex B.20 Free monad on `A + (−)²` is `Tree(A + −)` (binary trees with `A + Z`-labelled leaves) | App B.1 Ex B.20 | ✅ | `src/free_monad/tree_endo.rs` (`tree_to_free_mnd` + `free_mnd_to_tree` bijection) | Bijection witness `BinaryTree<A> ≅ FreeMnd<TreeEndo<A>, Infallible>`. The `Infallible` terminator (stable proxy for the never type `!`) statically forbids `Pure`-shaped leaves; all leaves come through the `Left(a)` summand. Verified in `tests/free_monad_bijections.rs` via three hand-built `BinaryTree` instances (leaf, single internal node, depth-3) round-tripping through `FreeMnd<TreeEndo, Infallible>`. |

### Appendix C — Additional Geometric Deep Learning Examples

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| C.1 Σₙ-equivariant learning on graphs (permutation group on `ℝⁿ × ℝⁿˣⁿ`) — leads to GNNs | App C.1 | ⏭️ | (deferred; sibling crate substrate) | Substrate exists in `catgraph` (graph types) and `catgraph-applied` (linear combinations); no cg-dl test fixture instantiating the full Σₙ-equivariant GDL recovery. Deferred to DL-3+ (would need a `PermutationGroupActionEndo<n>` instance + an `n × n` adjacency-matrix carrier). |
| C.2 SO(3)-equivariant learning on S² (rotation group on spherical fields) — leads to spherical CNNs | App C.2 | ⏭️ | (deferred; nalgebra-flavoured) | Linear-algebra heavy; deferred to DL-3+ until a `nalgebra` integration story lands (would need an `SO3GroupActionEndo` instance over an `S² → ℝ` carrier; the math:nalgebra-* skill family covers the matrix machinery). |
| C.3 G-equivariant learning on G (regular representation on `G → ℝ`) — leads to G-CNNs | App C.3 | ✅ | `src/algebra/group_action.rs` (`GroupActionEndo<G>` + `Z2Group` instance covers the `\|G\| = 2` case via the Ex 2.6 fixture) | The general-G version is naturally captured by the trait surface; the concrete `Z2Group` instance is the only shipped instance. Other groups (Σₙ, SO(3)) are the deferred C.1/C.2 rows above. |

### Appendix H — Weight Tying Examples

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Ex H.1 Linear equivariant layers for `Z₂` swap (concrete `2 × 2` matrix derivation; circulant ⇒ CNN) | App H.1 + Eq 11 | ⚠️ | `tests/algebra_homomorphisms.rs::test_z2_invariant_absolute_value` | The cg-dl test exercises the **structural** Z₂-equivariant claim via the abs-value algebra homomorphism (negation action → trivial action). The paper's H.1 worked example also provides a concrete `2 × 2` matrix derivation that the merged-paper rendering flags with a ⚠️ CAREFUL caveat (LHS as written reads as `Wᵀ(x₂, x₁)ᵀ` rather than `W(x₂, x₁)ᵀ` given the matrix convention; the symmetric-tied result `[[w₁, w₂], [w₂, w₁]]` is correct but the intermediate steps deserve fresh derivation). cg-dl deliberately implements only the structural correspondence (Ex 2.6) and does NOT depend on the H.1 index-bookkeeping arithmetic. |
| Rem H.2 Circulants and CNNs (general `Z_k`-translation equivariance ⇒ circulant matrix ⇒ CNN) | App H.2 | ➖ | (narrative + signal-processing background) | Background; the cg-dl `Z2Group` instance is the `k = 2` case. General-`k` not implemented (would need a `ZkGroup<k>` const-generic instance). |
| Ex H.3 Invariant maps as `(G × −)`-algebra hom with trivial-action codomain | App H.3 | ⚠️ | `tests/algebra_homomorphisms.rs::test_z2_invariant_absolute_value` | Same fixture as H.1 above; the absolute-value test exercises `f(g ▶ x) = f(x)` (invariance is the special case where target action is trivial). The merged-paper rendering flags the H.3 worked invariance derivation as inconsistent with a from-scratch re-derivation (correct constraint should yield columns-identical `[[w₁, w₁], [w₂, w₂]]`, the summary-document writes rows-identical). cg-dl test does NOT depend on this arithmetic. |
| Ex H.4 Streams as `(O × −)`-coalgebra | App H.4 + Eq H.4 | ⏭️ | (deferred; subsumed by Unfolding RNN) | No first-class `Stream<O>` type. The `UnfoldingRnn::unroll_to_vec` (App I.3 / J.2) covers the *bounded* case structurally; truly-infinite stream coalgebra semantics (lazy / `Iterator` / `tokio_stream::Stream` carrier) is deferred to DL-3+. |
| Ex H.5 Unfolds to streams as coalgebra homomorphisms | App H.5 | ⚠️ | `src/architectures/unfolding_rnn.rs` (`UnfoldingRnn::unroll_to_vec`) | Bounded-depth unfold; lazy `unroll_into_iter` deferred. The structural correspondence is exercised; the laziness is the open part. |
| Ex H.7 Moore machines as `(O × (I → −))`-coalgebra | App H.7 | ✅ | `src/architectures/moore_cell.rs` (`MooreCell` + `run`) | Full coalgebra structure; output emitted *before* consuming input (distinguishes Moore from Mealy). `run` exercises the coalgebra over a finite input list and asserts the output-then-step semantics on a `[0, 2, 4]` concrete fixture. |

### Appendix I — Parametric 2-endofunctors and their Algebras

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Ex I.1 Folding RNN cell — algebra of `Para(1 + A × −)` | App I.1 | ✅ | `src/architectures/folding_rnn.rs` (`FoldingRnn<P, A, S, Cell0, Cell1>::unroll`) | Two-piece decomposition `cell_0: P → S` + `cell_1: P × A × S → S` per the isomorphism `P × (1 + A × S) ≅ P + P × A × S`. Tested in `tests/architecture_unrollers.rs` (sum-with-bias + length-counter fixtures + `FreeMnd`-equivalence). |
| Ex I.2 Recursive NN cell — algebra of `Para(A + (−)²)` | App I.2 | ✅ | `src/architectures/recursive_nn.rs` (`RecursiveNn::unroll`) | Two-piece decomposition `cell_0: P → S` + `cell_1: P × A × S² → S`. Tested with three-tree post-order combine + `FreeMnd`-equivalence. The four-arg `cell_1` shape carries an `A` payload at internal-node combination — see §2.2 Ex 2.14 row above for the leftmost-leaf-payload convention. |
| Ex I.3 Unfolding RNN cell — coalgebra of `Para(O × −)` | App I.3 | ✅ | `src/architectures/unfolding_rnn.rs` (`UnfoldingRnn::unroll_to_vec`) | Two-piece decomposition `cell_o: P × S → O` + `cell_n: P × S → S`. Bounded-depth unrolling (`depth: usize` parameter); lazy/infinite deferred. |
| Ex I.4 Mealy cell / Full RNN cell — coalgebra of `Para(I → O × −)` | App I.4 | ✅ | `src/architectures/mealy_cell.rs` (`MealyCell::run`) | Two-stage closure shape `Cell: Fn((P, S)) -> Step` + `Step: FnOnce(I) -> (O, S)`. The "RNNs are learnable Mealy machines" framing from the paper is reified by the unroller. |
| Ex I.5 Moore cell — coalgebra of `Para(O × (I → −))` | App I.5 | ✅ | `src/architectures/moore_cell.rs` (`MooreCell::run`) | Output-then-step semantics: emit `cell_o(p, s)` BEFORE consuming next input. First emitted output is from initial state — distinguishes Moore from Mealy. |

### Appendix J — Unrolling Neural Networks via Transfinite Construction

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| Ex J.1 Iterated Folding RNN (algebra-hom unrolling with `Δ_P : P → P × P` reparameterization) | App J.1 | ✅ | `src/architectures/folding_rnn.rs::unroll` + `src/para/comonoid.rs::tie_weights` | The `Δ_P` reparameterization (paper "all timesteps share the same `P`") is operationally implemented by the cell carrying its `P` as a single field consumed at every step; weight tying via `tie_weights` is the explicit Δ application. |
| Ex J.2 Iterated Unfolding RNN (bounded-depth via `Stream` semantics in paper) | App J.2 | ⚠️ | `src/architectures/unfolding_rnn.rs::unroll_to_vec` | **Bounded-depth only.** Paper's `Stream` carrier is truly infinite (coinductive); cg-dl ships a `depth: usize` parameter. Truly-infinite final-coalgebra semantics deferred to DL-3+ (would need `Lazy<S>` / `Thunk<S>` / `tokio_stream::Stream` lift). Documented inline in CHANGELOG v0.2.0 Phase DL-2 Agent E notes. |
| Ex J.3 Iterated Recursive NN | App J.3 | ✅ | `src/architectures/recursive_nn.rs::unroll` | Post-order tree walk; weight-shared via single `P` per cell. |
| Ex J.4 Iterated Mealy cell | App J.4 | ✅ | `src/architectures/mealy_cell.rs::run` | Mealy-stream-process: thread state left-to-right through inputs, collect per-step outputs. |
| Ex J.5 Iterated Moore cell (alluded to but not numbered separately in paper App J) | App I.5 / "by symmetry with J.4" | ✅ | `src/architectures/moore_cell.rs::run` | Moore-stream-process; covered by App I.5 + the same `unroll` shape as J.4. |

### Appendix K — Consolidated Haskell Data Types & Cells

| Item | Paper ref | Status | Location | Notes |
|---|---|---|---|---|
| `data List a = Nil \| Cons a (List a)` (algebra for `1 + A × −`) | App K | ✅ | `src/free_monad/list_endo.rs` (via `Vec<A>` ↔ `FreeMnd<ListEndo<A>, ()>` bijection) | Vec is the operational `List`; the bijection witnesses the Haskell-data-type correspondence. |
| `data Tree a = Leaf a \| Node (Tree a) (Tree a)` (algebra for `A + (−)²`) | App K | ✅ | `src/free_monad/tree_endo.rs` (`BinaryTree<A>`) | Explicit Rust enum mirroring the Haskell definition. Bijection to `FreeMnd<TreeEndo<A>, Infallible>`. |
| `data Stream o = MkStream { output, next }` (coalgebra for `O × −`) | App K | ⚠️ | (deferred; bounded variant via `UnfoldingRnn`) | See App H.4 row — no first-class `Stream<O>` type yet; bounded variant ships in `UnfoldingRnn`. |
| `data Mealy o i = MkMealy { next: i → (o, Mealy o i) }` | App K | ✅ | `src/architectures/mealy_cell.rs` (`MealyCell` + two-stage closure) | The two-stage Rust closure shape is the standard workaround for "function-returning-closure" Haskell types. Type-level correspondence is preserved. |
| `data Moore o i = MkMoore { output: o, nextStep: i → Moore o i }` | App K | ✅ | `src/architectures/moore_cell.rs` (`MooreCell`) | Direct Rust analogue. |

---

## 3. Out of scope (v0.3.x)

- **Hopf-fibration / carry-operation conjecture** (transcript-only Dudzik claim about modular arithmetic with overflow as non-trivial S¹-fibration of S³ → S²). NOT in the published CDL ICML 2024 paper. NOT addressed in the published Filter Equivariants follow-up (arXiv:2507.08796, July 2025), which §6 explicitly flags ripple-carry as outside its framework. Private `hopf_fibration` namespace stub kept reserved; will not implement until a Dudzik preprint exists. See ⚠️ CAREFUL box at top of this document.
- **Truly-infinite final-coalgebra semantics** for `UnfoldingRnn` / `MealyCell` / `MooreCell` (lazy / `Iterator` / `tokio_stream::Stream` carrier). Bounded-depth variants ship in v0.2.0; lazy semantics deferred to DL-3+ if a consumer surfaces.
- **Non-`(Set, ×, 1)` `MonoidalCategory` instances** (R-module actegory, hyperdoctrine, vector-bundle, fibration-based). The trait surface admits them; concrete instances deferred to DL-3+. **v0.3.0 design surface (b)** addresses the blanket-impl-for-trivial-ZSTs / derive-macro question (forward-pointer to design doc).
- **Truly-rich monoid actions beyond `Z2Group`** (Σₙ permutation, SO(3) rotation, general `Z_k`, regular representation of arbitrary `G`). Deferred to DL-3+; see Appendix C.1 / C.2 rows.
- **First-class `NaturalTransformation<F, G>` type carrying naturality-square verification.** Documented obligation only; would land in DL-3+ if a consumer demands it.
- **First-class `Pointed<F>` / `Container` / `Polynomial` traits** for App B.3 well-pointedness, App K container framing. Documented obligations only.
- **Property-based testing** (proptest generators) for `verify_commutes` (algebra-hom commutativity) and `unroll == unroll_via_free_mnd` (FreeMnd-equivalence). Caller-sampled grids ship in v0.2.0; full proptest extension deferred to DL-3+.
- **`MonadAlgebraHom`-coherence machine-checking** (the unit + associativity laws of Def 2.3 beyond the F-algebra square). Caller-attested in v0.2.0; runtime-checked verifier deferred to DL-3+.
- **Filter Equivariants substrate (arXiv:2507.08796, July 2025) — semi-simplicial cones over `Δ^op`, `Bag` category, amalgamation algorithm.** Not part of the CDL ICML 2024 paper proper; explicitly flagged as a future-research substrate that *could* be folded into a `catgraph-coalition-dl` external sibling crate but is NOT in scope for catgraph-dl's current paper anchor. Cited here for forward-pointer completeness.
- **Active inference / Levin bioelectric / symbiogenesis substrate.** Belongs in a future external sibling `catgraph-coalition-dl`, not catgraph-dl. See workspace `current-plan.md` slot 4 (ambitious tier).

---

## 4. Acceptance-gate residuals

| Test | Target | Observed (v0.2.0) |
|---|---|---|
| `tests/comonoid_laws.rs` — coassociativity (proptest, 64 cases on `i32` + 64 on `String`) | exact equality | **all pass** (0 violations) |
| `tests/comonoid_laws.rs` — left counit (proptest on `i32`) | exact equality | **all pass** |
| `tests/comonoid_laws.rs` — right counit (proptest on `i32`) | exact equality | **all pass** |
| `tests/comonoid_laws.rs::test_tie_weights_smoke` — `(P × P, p1 + p2 + x)` tied at `p = 3, x = 5` | `11_i64` | **`11`** (exact) |
| `tests/algebra_homomorphisms.rs::test_z2_invariant_absolute_value` — `\|·\|` is Z₂-invariant on representative grid | `verify_commutes == true` | **all pass** (positive, negative, zero, empty Vec) |
| `tests/algebra_homomorphisms.rs::test_non_invariant_projection_fails` — `vec![x[0]]` is NOT Z₂-invariant (negative control) | `verify_commutes == false` | **as expected** (fails on at least one `(g, x)` pair) |
| `tests/free_monad_bijections.rs::roundtrip_vec` — proptest `Vec<u32>` round-trip via `vec_to_free_mnd ∘ free_mnd_to_vec` (64 cases per direction) | identity | **all pass** |
| `tests/free_monad_bijections.rs::roundtrip_tree` — three hand-built `BinaryTree` instances (leaf, single-internal-node, depth-3) round-trip via `tree_to_free_mnd ∘ free_mnd_to_tree` | identity | **all pass** |
| `tests/architecture_unrollers.rs::test_folding_rnn_freemnd_equivalence` — `unroll(cell, x) == unroll_via_free_mnd(cell, to_free_mnd(x))` for several samples | exact equality | **all pass** |
| `tests/architecture_unrollers.rs::test_recursive_nn_freemnd_equivalence` — same equivalence on the tree direction | exact equality | **all pass** |
| `tests/architecture_unrollers.rs::test_z2_invariant_folding` — Z₂-invariant absolute-value folding under `Z2Group` action | structural equality | **all pass** |
| `tests/para_composition.rs::test_compose_numeric` — `(P × Q ▶ X)` composition matches the canonical `(Q ⊗ P, h)` rule | exact equality | **all pass** |
| **Full test suite** (DL-2 v0.2.0) | **41 tests + 2 doctests + 5 ignored** | **all pass; 0 clippy warnings (`-W clippy::pedantic`); 0 doc warnings** |

---

## 5. Performance baseline (v0.2.0)

No criterion benchmarks ship with v0.2.0 (the crate is type-driven; runtime cost is dominated by the Rust closures and the consumer's choice of carrier types). All architecture unrollers are O(n) in input size with O(1) per-step overhead.

`cargo build -p catgraph-dl` clean compile in ~2.1s on a stock Rust 1.95 + Edition 2024 toolchain.

---

## 6. References

- **Paper (this anchor):** Gavranović, Lessard, Dudzik, von Glehn, Araújo, Veličković. *Categorical Deep Learning is an Algebraic Theory of All Architectures.* ICML 2024 PMLR 235:15209-15241. [arXiv:2402.15332v2](https://arxiv.org/abs/2402.15332). In-tree at `docs/2402.15332v2.pdf`.
- **Companion primer (in-tree):** [`2402.15332v2-SUMMARY.md`](2402.15332v2-SUMMARY.md) — merged primer combining (Part I) the transcript-vs-paper comparison flagging Hopf-fibration / carry / pathfinding as transcript-only and (Part II) the 1082-line faithful paper rendering with cross-checking caveats on App H.1 / H.3 worked-example arithmetic. Sourced from `~/Documents/sustia/categorical-dl/docs/categorical_deep_learning_transcript_vs_paper.md` + `categorical_deep_learning_merged.md` (no longer needed externally; the SUMMARY.md is the in-tree source of truth).
- **Follow-up (Dudzik et al. asynchronous NNs / 1-cocycles):** Dudzik, von Glehn, Pascanu, Veličković. *Asynchronous algorithmic alignment with cocycles.* Learning on Graphs Conference 2024. [arXiv preprint `[DvGPV24]`](https://arxiv.org/abs/2306.15632).
- **Follow-up (Filter Equivariants, July 2025; Dudzik co-author):** Lewis, Ghani, Dudzik, Perivolaropoulos, Pascanu, Veličković. *Filter Equivariant Functions: A symmetric account of length-general extrapolation on lists.* [arXiv:2507.08796v1](https://arxiv.org/abs/2507.08796). §6 explicitly acknowledges ripple-carry as outside the FE framework.
- **2-category Para foundations:** Cruttwell, Gavranović, Ghani, Wilson, Zanasi. *Categorical foundations of gradient-based learning.* Programming Languages and Systems, LNCS, 2022. Capucci, Gavranović, Hedges, Rischel. *Towards foundations of categorical cybernetics.* EPTCS 372:235-248, 2022.
- **Free monads (App B.1):** Adámek, Milius, Moss. *Initial algebras and terminal coalgebras: a survey.* And: Aczel, Mendler. *A final coalgebra theorem.*
- **Workspace audit companions:** `catgraph/docs/FS19-AUDIT.md` (Fong-Spivak 2019 hypergraph categories), `catgraph-applied/docs/FS18-AUDIT.md` (Fong-Spivak 2018 Seven Sketches), `catgraph-magnitude/docs/BV25-AUDIT.md` (Bradley-Vigneaux 2025 magnitude), [`irreducible/docs/GORARD23-AUDIT.md`](https://github.com/tsondru/irreducible/blob/main/docs/GORARD23-AUDIT.md) (Gorard 2023 functorial irreducibility).
- **HKT pattern reference:** `causality:hkt-type-system` skill (rebozo plugin v1.2.0) — `deep_causality_haft` v0.3.1 GAT-based HKT pattern. The cg-dl `EndoFunctor::Apply<X>`, `MonoidalCategory::Tensor<A, B>`, and `Actegory::ActionResult<P, X>` follow the same GAT-witness pattern documented there.

---

## 7. Forward-look — v0.3.0 + v0.3.1 shipped 2026-05-06; v0.4.0 deferrals below

Three flagged surfaces from v0.2.0 + a Hopf-fibration evidence-note update **shipped at v0.3.0** (catgraph-dl-v0.3.0 + workspace umbrella `v0.13.1` at SHA `ac699f6`, 2026-05-06). 8 Important findings + ~10 Minor + 6 Architectural surfaced by the post-shipping multi-reviewer pass; 8 Important + 5 ride-along Minors **shipped at v0.3.1** (catgraph-dl-v0.3.1 + workspace umbrella `v0.13.2` at SHA `12ad5fd`, 2026-05-06); 6 Architectural folded forward to v0.4.0. v0.3.0 + v0.3.1 deltas populated below.

Shipped at v0.3.0 (deltas captured below):

- Surface (a) ✅ — `MonoidalCategory` `&self` module-level rustdoc paragraph "Why methods take `&self`".
- Surface (b) ✅ — `SetCategoryDefaults` opt-in marker trait + blanket `MonoidalCategory` impl. Took option (γ-ii) blanket-via-opt-in-marker; (γ-i) sub-trait-with-default-method-bodies does not type-check on stable Rust.
- Surface (c) ✅ — `Actegory` / `Para` / `tie_weights` consumption-pathway documentation in `src/para/comonoid.rs` + `tests/coalition_consumption_simulation.rs` end-to-end simulation.
- Surface 4 ✅ — Hopf-fibration evidence-note update (Filter Equivariants §6 confirms no preprint exists; private namespace stub kept reserved per locked option (i)).

Shipped at v0.3.1 patch (deltas captured below):

- ✅ `SetCategoryDefaults: Sized` bound + ⚠️ Conflict-guard rustdoc (R-Imp-1+2).
- ✅ `SetCategoryDefaults` doctest extended to all 5 method bodies (F-Imp-3).
- ✅ `lib.rs` Scope + Out-of-scope sections refreshed for v0.3.x reality (F-Imp-1).
- ✅ `catgraph-dl/CLAUDE.md` Para-upstream-of-Quantale section now references the integration test (F-Imp-2).
- ✅ Workspace `CLAUDE.md` umbrella table v0.13.1 SHA backfill + v0.13.2 row + sibling-repo descriptor refresh (P-Imp-1, P-Imp-3).
- ✅ Workspace `current-plan.md` slot list restructured with new slot 2 for catgraph-coalition v0.4.0 Phase 6E (P-Imp-2).
- ✅ Ride-along Minors: `actegory.rs` `&self` cross-link, audit-doc out-of-scope header v0.2.x → v0.3.x, module-vs-trait rustdoc consolidation, CHANGELOG date format hyphen-space.
- ✅ Reviewer substitution per workspace `CLAUDE.md` release rule 7 case (b) flagged in v0.3.0 + v0.3.1 CHANGELOG (`superpowers:code-reviewer` unavailable; substituted with `feature-dev:code-reviewer`).

Deferred to v0.4.0 (6 architectural items folded into the v0.4.0 forward-look):

- **`tie_weights` actegory-genericity** — v0.3.x `tie_weights` signature is hardcoded to `ParaMorphism<SetMonoidal, SetActegory, …>`. v0.4.0 (catgraph-coalition consumer side) will need either (a) cg-dl-side `tie_weights` generalisation to `<C: Actegory<SetMonoidal>>` OR (b) coalition-side wrapper around `SetActegory`. Architectural; not Blocking. Captured in v0.3.0 + v0.3.1 CHANGELOG Notes.
- **HKT `&self` runtime-payload audit** at the future `QuantaleActegory` impl site — validates whether the v0.3.x `&self` divergence-from-HAFT rationale is genuinely load-bearing.
- **`tie_weights` `P: Clone` bound relaxation** for BTV21 substitution-grammar parameters that may be heap-shared `Arc<...>`.
- **External-user coherence-footgun rustdoc tightening** for `SetCategoryDefaults` — partially closed in v0.3.1 with the conflict-guard rustdoc; v0.4.0 may evaluate sealing pattern.
- **H.4 `[patch]` block completeness in `irreducible`** — future-trap if cg-dl ever lands as an irreducible dep.
- **Pin-bump chain forward planning** — coordinate cg-mag v0.3.0 (workspace slot 1) + cg-coalition v0.4.0 (slot 2) sequencing.
- **Lax / strong / strict `MonoidalCategory` trait family** — current trait is silently strict; v0.3.x ships rustdoc explaining the `&self` deferral slot. The trait-level distinction (`LaxMonoidalCategory` / `StrongMonoidalCategory` / `StrictMonoidalCategory` family) lands when a non-`(Set, ×, 1)` consumer surfaces.
- **Hopf-fibration body** — held until Dudzik publishes a preprint. Filter Equivariants §6 (July 2025) confirms ripple-carry is outside the FE framework; no Hopf paper exists as of audit date.

---

## v0.2.0 deltas (2026-05-02 Phase DL-2)

Initial audit at v0.2.0 baseline. 76 audited items; 84% DONE on the implementable surface (47 DONE / 6 PARTIAL / 3 DEFERRED out of 56 implementable; 20 N/A).

## v0.3.0 deltas (2026-05-06 Phase DL-3 — flagged surfaces + Hopf evidence note)

| Item | Spec | Audit row affected |
|---|---|---|
| **Surface (a) — `MonoidalCategory` `&self` rationale rustdoc** | New module-level rustdoc paragraph "Why methods take `&self`" in `src/para/monoidal_category.rs` (CHANGELOG `[0.3.0]` Documentation bullet). Documents the deliberate divergence from HAFT v0.3.1's static-dispatch convention: `&self` is reserved for DL-3+ instances over richer monoidal categories (R-module / hyperdoctrine / vector-bundle) that will carry runtime data — freezing the trait at static methods today would force a breaking change later. | §3.2 lax/strong/strict row — notes updated to reflect rustdoc-level documentation improvement; status remains ⚠️ PARTIAL (the trait-level lax / strong / strict distinction itself is still deferred to DL-4+). |
| **Surface (b) — `SetCategoryDefaults` opt-in marker + blanket impl** | New `pub trait SetCategoryDefaults {}` plus `impl<T: SetCategoryDefaults> MonoidalCategory for T` in `src/para/monoidal_category.rs`. Re-exported at `catgraph_dl::para::SetCategoryDefaults`. `SetMonoidal` opts in via empty `impl SetCategoryDefaults for SetMonoidal {}`. The blanket fixes `Object = SetObject`, `Morphism = SetMorphism`, `Unit = ()`, `Tensor<A, B> = (A, B)` and supplies all five method bodies (`tensor_objects` / `unit` / `associate` / `left_unitor` / `right_unitor`) as the canonical Cartesian-product / tuple-re-association forms. Implementation took option (γ-ii) blanket-via-opt-in-marker; option (γ-i) sub-trait-with-default-method-bodies does not type-check on stable Rust because a sub-trait cannot override supertrait method bodies. New doctest in `SetCategoryDefaults` rustdoc. | §3.1 — **new audited row added** (`(Set, ×, 1)`-flavoured ZST defaults pattern, marked ✅ DONE). §3.1 row count widened from 6/0/0/1/7 to 7/0/0/1/8. Total widened from 47/6/3/20/76 to 48/6/3/20/77. The paper does not name this pattern explicitly, but it operationalises CDL §3.1's choice of `(Set, ×, 1)` as the default monoidal category — the new audit row tracks that operationalisation. |
| **Surface (c) — Consumption-pathway rustdoc + simulation test** | Three new module-level rustdoc paragraphs in `src/para/comonoid.rs` covering (i) the layering invariant ("Para is upstream of Quantale"; `catgraph-coalition` v0.4.0 imports `Actegory`, never re-defines it), (ii) what the v0.4.0 call site will look like end-to-end, (iii) what the actegory ▶ widening means for `tie_weights` in non-`(Set, ×, 1)` actegories. New `tests/coalition_consumption_simulation.rs` defines a local `MockQuantale: Actegory<SetMonoidal>` ZST and exercises `tie_weights::<i64, _, i64, i64>(3, untied)` end-to-end. Cross-validates `MockQuantale::act` against `SetActegory::act` pointwise (the consumption pathway is structure-agnostic). | §3.1 Theorem G.10 row — notes extended with the `coalition_consumption_simulation.rs` end-to-end simulation reference + the `SetActegory`-bound caveat (actegory-genericity deferred to v0.4.0+). The `tie_weights` API status remains ✅ DONE; simulation hardens the API contract against future drift. **Architectural finding for v0.4.0+** (captured in §7 forward-look + CHANGELOG Notes): generalising `tie_weights` to `<C: Actegory<SetMonoidal>>` is a v0.4.0-time decision; coalition v0.4.0 will need either cg-dl-side generalisation or coalition-side wrapper. |
| **Surface 4 — Hopf-fibration evidence-note update** | `src/hopf_fibration/mod.rs` + `src/lib.rs` ⚠️ CAREFUL section + `CLAUDE.md` ⚠️ CAREFUL section updated with the 2026-05-06 evidence note: the Filter Equivariants follow-up paper ([arXiv:2507.08796v1](https://arxiv.org/abs/2507.08796), Lewis-Ghani-Dudzik-Perivolaropoulos-Pascanu-Veličković, July 2025) §6 explicitly puts ripple-carry addition **outside** the FE framework: "Ripple-carry addition is another interesting example that clearly does not fit our FE framework, in particular because its local behavior is not independent across subproblems". As of 2026-05-06 no Hopf-fibration / carry-operation preprint exists; the cited `[DvGPV24]` reference in FE is the existing *Asynchronous algorithmic alignment with cocycles* (LoG 2024, already cited in CDL §3.2), not a new Hopf paper. Locked option (i) keep-stub. | §3 "Out of scope (v0.2.x)" Hopf-fibration bullet — evidence trail extended; no audit-row status flips. The `hopf_fibration` private namespace stub remains kept-and-reserved with no public API. The audit-doc top-of-file ⚠️ CAREFUL provenance box was updated at v0.2.0 baseline to reference the FE evidence; v0.3.0 propagates the note into source-tree rustdoc + `CLAUDE.md`. |
| **Internal hoist (no externally observable change)** | `impl MonoidalCategory for SetMonoidal { ... }` (v0.2.0 hand-written) hoisted into the `SetCategoryDefaults` blanket impl. All v0.2.0 tests + doctests continue to pass without modification — pointwise-identical method bodies. | None — code-organisation hoist only. |
| **Test counts** | 42 unit + integration tests (v0.2.0: 41; +1 `tie_weights_consumption_pathway_simulation`). 3 doctests + 5 ignored (v0.2.0: 2 + 5; +1 `SetCategoryDefaults` doctest). 0 clippy pedantic warnings; 0 doc warnings. | §4 acceptance-gate residuals row "Full test suite" — counts updated implicitly via CHANGELOG; all v0.2.0 rows continue to pass at v0.3.0. |

**Headline-numbers update at v0.3.0:** 84% DONE on the implementable surface (now 48 DONE / 6 PARTIAL / 3 DEFERRED out of 57 implementable; 20 N/A). Tightened from 47/56 = 83.93% (v0.2.0) to 48/57 = 84.21% (v0.3.0); both round to 84%.

## v0.3.1 deltas (2026-05-06 Phase DL-3 multi-reviewer pass — 8 Important findings + ride-along Minors)

**First cg-dl release through the workspace `CLAUDE.md` release rule 7 canonical reviewer triumvirate + deep paper-audit reviewer.** Reviewer substitution flag (release rule 7 case (b)): `superpowers:code-reviewer` was unavailable in the current environment; substituted with `feature-dev:code-reviewer`. The other three triumvirate seats (`rust-v2:rust-dev-v2`, `rust-v2:rust-practical`) plus the additive deep paper-audit reviewer (`general-purpose` briefed with the in-tree primer + paper PDF + source tree) ran as designed. The substitution did not affect the v0.3.0 ship; the post-shipping pass surfaced 8 Important + ~10 Minor + 6 Architectural findings, all triaged into v0.3.1 + the v0.4.0 forward-look doc.

**Strictly additive on v0.3.0** — no API break; behaviour pointwise identical for all v0.3.0 callers; the only bound widening is `SetCategoryDefaults: Sized`, satisfied by every shipping caller (`SetMonoidal` and the doctest's `MyMonoidal` are unit structs).

| Item | Spec | Audit row affected |
|---|---|---|
| **R-Imp-1 + R-Imp-2** (rust-v2:rust-dev-v2 lens) — `SetCategoryDefaults: Sized` bound + ⚠️ conflict-guard rustdoc | The `SetCategoryDefaults` trait now carries a `: Sized` supertrait bound (was: empty bound list) — soft witness that the trait is intended for zero-sized witness types. A downstream attempt at `impl SetCategoryDefaults for &'a [u8]` will fail at the bound site rather than silently picking up the blanket. New "## ⚠️ Conflict-guard caveat" rustdoc section explains that combining `impl SetCategoryDefaults for MyType {}` with a hand-rolled `impl MonoidalCategory for MyType { ... }` will hit a `conflicting implementations` compile error — the convention is "don't combine the two": opt into `SetCategoryDefaults` for `(Set, ×, 1)` flavour, or write `impl MonoidalCategory` directly for non-`(Set, ×, 1)` flavour. | §3.1 — existing v0.3.0 `SetCategoryDefaults` row notes refined; status remains ✅ DONE. The bound + rustdoc tightening do not flip any other audit row. |
| **F-Imp-3** (feature-dev:code-reviewer lens, substituting for unavailable superpowers:code-reviewer per release rule 7 case (b)) — `SetCategoryDefaults` doctest extended to all 5 method bodies | The v0.3.0 doctest exercised only `tensor_objects` + `unit` (2 of 5 method bodies). v0.3.1 extends to all 5: `assert_eq!(m.tensor_objects(1_i32, "two"), (1_i32, "two"))`, `assert_eq!(m.unit(), ())`, `assert_eq!(m.associate(((1_i32, "two"), 3.0_f64)), (1_i32, ("two", 3.0_f64)))`, `assert_eq!(m.left_unitor::<i32>(((), 42_i32)), 42_i32)`, `assert_eq!(m.right_unitor::<i32>((99_i32, ())), 99_i32)`. Downstream users opening `cargo doc` now see a runnable demonstration that the blanket impl handles the coherence isomorphisms correctly. | §3.1 — existing v0.3.0 `SetCategoryDefaults` row notes extended (doctest coverage now 5/5 = 100% of method bodies); status remains ✅ DONE. |
| **F-Imp-1** (feature-dev lens) — `lib.rs` Scope + Out-of-scope sections refreshed | The crate-level rustdoc "Scope (v0.1.0 — Phase DL-1 scaffold)" header + "Out of scope for v0.1.0" bullets were both stale at v0.3.0 (Phase DL-2 + DL-3 had shipped). v0.3.1 renames "Scope (v0.1.0)" → "Scope" with a per-module breakdown citing v0.2.0 + v0.3.0 surfaces, and replaces "Out of scope for v0.1.0" with "Deferred surfaces (Phase DL-4+)" listing 6 candidate items including the `tie_weights` actegory-generalisation forward-pointer. | None — code-organisation refresh of the crate-root rustdoc; no audit-row status flips. |
| **F-Imp-2** (feature-dev lens) — `catgraph-dl/CLAUDE.md` Para-upstream-of-Quantale section now references the integration test | The v0.3.0 design doc §2.3 surface (c) action item 3 ("Update CLAUDE.md '⚠️ CAREFUL — Para is upstream of Quantale' section to reference the new integration test and the consumption-pathway rustdoc") did not ship in v0.3.0. v0.3.1 closes the gap: the section now points at `tests/coalition_consumption_simulation.rs` + `src/para/comonoid.rs` "Consumption pathway" rustdoc as authoritative for downstream coalition v0.4.0 authors. | §3.1 Theorem G.10 row — notes already include the simulation-test reference (v0.3.0 deltas); v0.3.1 propagates the cross-reference into `CLAUDE.md` for first-stop discoverability. Status remains ✅ DONE. |
| **P-Imp-1 + P-Imp-3** (rust-v2:rust-practical lens, cross-repo dependency mechanics) — workspace `CLAUDE.md` umbrella table + sibling-repo descriptors | Workspace `CLAUDE.md`: `v0.13.1` row SHA placeholder `TBD` filled with `ac699f6`; new `v0.13.2` row added (this v0.3.1 release at `12ad5fd`). Sibling-repo descriptors refreshed: `irreducible` was listed at "v0.6.2; pin bump to v0.13.0 pending" (stale on both counts) — corrected to "v0.6.5; pinned to umbrella v0.13.0; does not consume catgraph-dl"; `catgraph-coalition` now flags the v0.13.2 pin-bump as a Phase 6E prerequisite. | None — workspace-level documentation; not in this audit doc's scope but tracked here for cross-repo coordination completeness. |
| **P-Imp-2** (rust-v2:rust-practical lens) — workspace `current-plan.md` slot list restructured | Previous slot 1 (cg-dl v0.3.0) marked DONE with both v0.3.0 + v0.3.1 SHAs. **New slot 2** added for `catgraph-coalition` v0.4.0 Phase 6E (Quantale-actegory CatDL substrate consumption — sibling-repo work, not a workspace tag); the opening Phase 6E task is the pin-bump from workspace umbrella `v0.13.0` to `v0.13.2`. Slots 3 (cg-coalition v0.5.0 generic-Weight) + 4 (cg-coalition-dl v0.1.0 ambitious tier) renumbered. | §3 Out of scope row "BTV 2021 substitution-grammar enrichment" — note refined: forward-pointer to coalition v0.4.0 Phase 6E. Status unchanged. |
| **F-Min-4** (feature-dev lens, ride-along) — `actegory.rs` `&self` rationale cross-link | Surface (a) added a "Why methods take `&self`" module-level paragraph in `monoidal_category.rs`; `actegory.rs` carried the same `&self` design for the same reason but had no analogous note. v0.3.1 adds a one-paragraph cross-link in `actegory.rs` so a reader of that file in isolation finds the future-proofing rationale without manual navigation. | §3.1 row — Actegory rustdoc completeness; status unchanged. |
| **F-Min-5 / A-Min-2** (feature-dev + paper-audit lenses, ride-along) — audit-doc "## 3. Out of scope" header drift | Section header was frozen at "(v0.2.x)" while the rest of the doc had been updated for v0.3.0 (header, summary table, deltas section). v0.3.1 relabels to "(v0.3.x)". | This audit doc — section-header version drift closed. |
| **R-Min-3** (rust-v2:rust-dev-v2 lens, ride-along) — module-vs-trait rustdoc consolidation | The v0.3.0 (γ-ii) implementation note was duplicated at module-level (`monoidal_category.rs` lines 54-73) and trait-level (`SetCategoryDefaults` rustdoc lines 158-173). v0.3.1 trims the module-level paragraph to a one-line pointer at the trait-level home; the (γ-ii) discussion now lives at exactly one canonical site. | None — code-organisation hoist; status unchanged. |
| **P-Min-1** (rust-v2:rust-practical lens, ride-along) — CHANGELOG date format | v0.3.0 entry used em-dash `[0.3.0] — 2026-05-06` style departing from Keep-a-Changelog `[X.Y.Z] - YYYY-MM-DD`. v0.3.1 entry uses the standard hyphen-space form; v0.3.0 entry retroactively changed to match. | None — CHANGELOG style; not in this audit doc's scope. |
| **Test counts** | 42 unit + integration tests (unchanged from v0.3.0); 3 doctests + 5 ignored (unchanged from v0.3.0; the `SetCategoryDefaults` doctest body grew but the doctest count is unchanged). 0 clippy pedantic warnings; 0 doc warnings. | §4 acceptance-gate residuals — counts unchanged from v0.3.0. |
| **Architectural findings folded forward to v0.4.0** | 6 items captured in the v0.4.0 forward-look: (1) `tie_weights` actegory-generalisation, (2) HKT `&self` runtime-payload audit at downstream actegory impl sites, (3) `tie_weights` `P: Clone` bound relaxation for BTV21, (4) external-user coherence-footgun rustdoc tightening (sealing pattern as an option), (5) `[patch]` block completeness in downstream consumers, (6) pin-bump chain forward planning. | §7 forward-look — restructured: v0.3.0 forward-look replaced by v0.4.0 forward-look; design-phase trigger conditions documented. |

**Headline-numbers at v0.3.1:** **unchanged from v0.3.0** — 48 DONE / 6 PARTIAL / 3 DEFERRED out of 57 implementable (84.21%). v0.3.1 fixes are rustdoc-tightenings, bound additions, doctest extensions, header refreshes, and CHANGELOG / README / current-plan updates; no new audit rows added; no row status flips. The audit's paper-coverage claim is unchanged at v0.3.1; what changed is the **completeness of the surrounding documentation** (rustdoc, doctest coverage, CLAUDE.md cross-references) such that reviewers now find the existing audit rows easier to verify.

[Unreleased]: forward-look section above
