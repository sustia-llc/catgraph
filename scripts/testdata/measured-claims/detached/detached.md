# Numbers not touching their marker — must FAIL

A decimal must not be read as its fractional digits. This is the B2 false
green: `0.7828` once passed as 7828.

The ratio rose to 0.7828<!--m:fixture.plain--> in the new corpus.

A correct one keeps the no-citations check from firing instead:
**0**<!--m:fixture.zero-->.
