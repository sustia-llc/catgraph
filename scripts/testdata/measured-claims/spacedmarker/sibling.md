# The sibling that keeps the no-citations rule quiet

Without this file, `only.md` alone would fail for the wrong reason — the
"prose cited nothing" rule rather than the drift — and the case would pass
while B1 stayed live.

A correct citation: **0**<!--m:fixture.zero--> mismatches.
