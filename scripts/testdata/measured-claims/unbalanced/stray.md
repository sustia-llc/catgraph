# An unterminated fence must be an error, not a silent skip

A correct citation first, so the no-citations rule is not what fires:
**0**<!--m:fixture.zero--> mismatches.

Now a stray opener that is never closed:

```text

Everything below here was silently skipped before the fence was required to
balance — including this WRONG citation, which is the false green:

**9 999**<!--m:fixture.plain--> against a measured 7828.
